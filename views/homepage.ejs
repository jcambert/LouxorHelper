
<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Inventaire ATF</a>
            <span> {{$root.user.firstname}} {{$root.user.lastname}}</span>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <form class="navbar-form navbar-right" ng-submit="trySignin()">
            <div class="form-group"  ng-show="!$root.user.isLogged">
                <input type="text" placeholder="Email" class="form-control" ng-model="user.email">
            </div>
            <div class="form-group" ng-show="!$root.user.isLogged">
                <input type="password" placeholder="Password" class="form-control" ng-model="user.password">
            </div>
            <button type="submit" class="btn btn-success" ng-show="!$root.user.isLogged">Sign in</button>
            <button type="button" class="btn btn-success" ng-show="$root.user.isLogged" ng-click="logout()">Sortir</button>
            </form>
            
           
        </div><!--/.navbar-collapse -->
    </div>
</nav>
<div class="container" role="main">
    <ui-view></ui-view>
</div>

