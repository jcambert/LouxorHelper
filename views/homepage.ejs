

    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" ui-sref="home.accueil"><img ng-src="images/atf-logo5.jpg"></img></a>
                <span class="user-name"  ng-show="$root.isLogged"> {{$root.user.username}}</span>
            </div>
            <div id="navbar" class="navbar-collapse collapse">
                <form class="navbar-form navbar-right" ng-submit="trySignin()">
                    <div class="form-group"  ng-show="!$root.isLogged">
                        <input type="text" placeholder="Email" class="form-control" ng-model="user.email">
                    </div>
                    <div class="form-group" ng-show="!$root.isLogged">
                        <input type="password" placeholder="Password" class="form-control" ng-model="user.password">
                    </div>
                    <button type="submit" class="btn btn-success" ng-show="!$root.isLogged">Entrez</button>
                    <button type="button" class="btn btn-success" ng-show="$root.isLogged" ng-click="$root.logout()">Sortir</button>
                    <button  class="btn btn-success" ng-show="!$root.isLogged" ui-sref="home.signup">Enregistrez-vous</button>
                </form>
                
            
            </div><!--/.navbar-collapse -->
        </div>
    </nav>
    <div class="container-fluid has-navbar-top" role="main">
        <ui-view></ui-view>
    </div>
